<%= turbo_stream.remove "attachment-#{params[:id]}" %>
<%= turbo_stream.update "flash-messages" do %>
  <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
    画像を削除しました
  </div>
<% end %>
