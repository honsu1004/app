<div class="max-w-2xl mx-auto mt-10 p-6 bg-white rounded-xl shadow">
  <h1 class="text-2xl font-bold mb-6 text-center">旅行プランを編集</h1>
  <%= render "form", plan: @plan %>
</div>
<%= link_to "削除", plan_path(@plan), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "text-red-600 hover:text-red-800 font-semibold" %>
