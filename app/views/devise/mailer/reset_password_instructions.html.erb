<p>こんにちは <%= @resource.name %>さん！</p>

<p>あなたのパスワードを変更するためのリンクのリクエストがありました。以下のリンクから変更できます。</p>

<p><%= link_to 'パスワードを変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>このリクエストをしていない場合は、このメールを無視してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまで、あなたのパスワードは変更されません。</p>
